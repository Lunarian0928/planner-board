export class BoardInfo {
    constructor(textContent, creationTime, modificationTime) {
        this.textContent = textContent;
        this.creationTime = creationTime;
        this.modificationTime = modificationTime;
    }
}